diff -rupN orig/src/plugins/gstreamer/mediaplayer/qgstreamerplayersession.cpp git/src/plugins/gstreamer/mediaplayer/qgstreamerplayersession.cpp
--- orig/src/plugins/gstreamer/mediaplayer/qgstreamerplayersession.cpp	2019-08-13 13:26:42.672070440 -0600
+++ git/src/plugins/gstreamer/mediaplayer/qgstreamerplayersession.cpp	2019-08-13 14:35:47.690506992 -0600
@@ -134,6 +134,7 @@ QGstreamerPlayerSession::QGstreamerPlaye
      m_videoProbe(0),
      m_audioProbe(0),
      m_volume(100),
+	 m_latency(200),
      m_playbackRate(1.0),
      m_muted(false),
      m_audioAvailable(false),
@@ -352,6 +353,7 @@ void QGstreamerPlayerSession::loadFromUr
         emit tagsChanged();
 
         g_object_set(G_OBJECT(m_playbin), "uri", m_request.url().toEncoded().constData(), NULL);
+		g_signal_connect(G_OBJECT(m_playbin), "source-setup", G_CALLBACK(handleSourceCreated), this);
 
         if (!m_streamTypes.isEmpty()) {
             m_streamProperties.clear();
@@ -1761,6 +1763,15 @@ GstAutoplugSelectResult QGstreamerPlayer
     return res;
 }
 
+void QGstreamerPlayerSession::handleSourceCreated(GstElement *pipe, GstElement *source, QGstreamerPlayerSession *session)
+{
+    Q_UNUSED(pipe);
+
+    if (session->m_isLiveSource)
+        g_object_set(source, "latency", session->m_latency, NULL);
+}
+
+
 void QGstreamerPlayerSession::handleElementAdded(GstBin *bin, GstElement *element, QGstreamerPlayerSession *session)
 {
     Q_UNUSED(bin);
diff -rupN orig/src/plugins/gstreamer/mediaplayer/qgstreamerplayersession.h git/src/plugins/gstreamer/mediaplayer/qgstreamerplayersession.h
--- orig/src/plugins/gstreamer/mediaplayer/qgstreamerplayersession.h	2019-08-09 15:11:44.654771193 -0600
+++ git/src/plugins/gstreamer/mediaplayer/qgstreamerplayersession.h	2019-08-13 14:34:52.290161786 -0600
@@ -180,6 +180,7 @@ private:
 #if !GST_CHECK_VERSION(1,0,0)
     static void insertColorSpaceElement(GstElement *element, gpointer data);
 #endif
+    static void handleSourceCreated(GstElement *pipe, GstElement *source, QGstreamerPlayerSession *session);
     static void handleElementAdded(GstBin *bin, GstElement *element, QGstreamerPlayerSession *session);
     static void handleStreamsChange(GstBin *bin, gpointer user_data);
     static GstAutoplugSelectResult handleAutoplugSelect(GstBin *bin, GstPad *pad, GstCaps *caps, GstElementFactory *factory, QGstreamerPlayerSession *session);
@@ -230,6 +231,7 @@ private:
     QGstreamerAudioProbeControl *m_audioProbe;
 
     int m_volume;
+    int m_latency;
     qreal m_playbackRate;
     bool m_muted;
     bool m_audioAvailable;
