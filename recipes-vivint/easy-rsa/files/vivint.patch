# HG changeset patch
# User Johny Bui <jbui@vivint.com>
# Date 1396502721 21600
# Node ID a8d9322d7c65551144c5dc8651efcf7069e31405
# Parent  e0cd7b7a6c8922f864de15651a6678e5d709fab6
mod for vivint use

diff --git a/build-ca b/build-ca
--- a/build-ca
+++ b/build-ca
@@ -5,4 +5,4 @@
 #
 
 export EASY_RSA="${EASY_RSA:-.}"
-"$EASY_RSA/pkitool" --interact --initca $*
+"$EASY_RSA/pkitool"  --initca $*
diff --git a/build-key b/build-key
--- a/build-key
+++ b/build-key
@@ -4,4 +4,4 @@
 # root certificate.
 
 export EASY_RSA="${EASY_RSA:-.}"
-"$EASY_RSA/pkitool" --interact $*
+"$EASY_RSA/pkitool"  $*
diff --git a/build-key-server b/build-key-server
--- a/build-key-server
+++ b/build-key-server
@@ -7,4 +7,4 @@
 # extension in the openssl.cnf file.
 
 export EASY_RSA="${EASY_RSA:-.}"
-"$EASY_RSA/pkitool" --interact --server $*
+"$EASY_RSA/pkitool" --server $*
diff --git a/vars b/vars
--- a/vars
+++ b/vars
@@ -36,7 +36,7 @@
 # a rm -rf on this directory
 # so make sure you define
 # it correctly!
-export KEY_DIR="$EASY_RSA/keys"
+export KEY_DIR="/media/extra/conf/openvpn_server/easy-rsa/keys"
 
 # Issue rm -rf warning
 echo NOTE: If you run ./clean-all, I will be doing a rm -rf on $KEY_DIR
@@ -50,23 +50,23 @@
 # down TLS negotiation performance
 # as well as the one-time DH parms
 # generation process.
-export KEY_SIZE=2048
+export KEY_SIZE=1024
 
 # In how many days should the root CA key expire?
-export CA_EXPIRE=3650
+export CA_EXPIRE=36500
 
 # In how many days should certificates expire?
-export KEY_EXPIRE=3650
+export KEY_EXPIRE=36500
 
 # These are the default values for fields
 # which will be placed in the certificate.
 # Don't leave any of these fields blank.
 export KEY_COUNTRY="US"
-export KEY_PROVINCE="CA"
-export KEY_CITY="SanFrancisco"
-export KEY_ORG="Fort-Funston"
-export KEY_EMAIL="me@myhost.mydomain"
-export KEY_OU="MyOrganizationalUnit"
+export KEY_PROVINCE="UT"
+export KEY_CITY="Lehi"
+export KEY_ORG="Vivint"
+export KEY_EMAIL=""
+export KEY_OU="Innovation"
 
 # X509 Subject Field
 export KEY_NAME="EasyRSA"
