# Instructions for flashing the image
* cd to this directory
* run:
`~$ sudo ./uuu <archive-name>.zip`
* **savedfiles.tar** will be downloaded from the target. It contains an archived /media/bootscript/ folder (serial numbers). Use for manual recovery if needed.
